នៅ ASP.NET Core MVC, **Action Selector** គឺជាឧបករណ៍សំខាន់សម្រាប់កំណត់ថា **action method** មួយនៅក្នុង **controller** ត្រូវតែត្រូវបានហៅ ឬមិនត្រូវបានហៅ ដោយ system ពី HTTP request ។ វាត្រូវបានអនុវត្តជាមធ្យម **attributes** នៅលើ **action methods**។ វាអាចជួយគ្រប់គ្រង routing និង behavior របស់ method ផ្ទាល់ខ្លួនបានយ៉ាងច្បាស់។

Action Selector ចែកបានជា ៣ ប្រភេទដូចខាងក្រោម៖

---

### 1️⃣ **[ActionName]**

- ប្រើសម្រាប់ **ប្ដូរឈ្មោះ action method** នៅ routing, ដោយឲ្យ HTTP request អាចហៅ method ដោយឈ្មោះផ្សេងពី method name ដើម។
- Syntax:

```csharp
[ActionName("CustomName")]
public IActionResult OriginalMethodName()
{
    return Content("This is OriginalMethodName, but called as CustomName.");
}
```

**Example Usage:**

```csharp
public class ProductController : Controller
{
    [ActionName("List")]
    public IActionResult GetAllProducts()
    {
        // Method name: GetAllProducts
        // Route name: /Product/List
        return View();
    }
}
```

> ក្រោមលទ្ធផលនេះ, URL `https://localhost:5001/Product/List` នឹងហៅ method `GetAllProducts()`។

---

### 2️⃣ **[NonAction]**

- ប្រើដើម្បី **កំណត់ method មួយនៅក្នុង controller ជា non-action**, មានន័យថា **មិនអាចហៅពី HTTP request បាន**។
- Syntax:

```csharp
[NonAction]
public void HelperMethod()
{
    // មិនអាចហៅជា HTTP action
}
```

**Example Usage:**

```csharp
public class OrderController : Controller
{
    [NonAction]
    public int CalculateDiscount(int total)
    {
        return total > 100 ? 10 : 0;
    }

    public IActionResult Checkout()
    {
        int discount = CalculateDiscount(150); // internal use only
        return View();
    }
}
```

> Method `CalculateDiscount` មិនអាចចូលដំណើរការពី browser ឬ HTTP request បានទេ។

---

## 🔹 HTTP Verb Attributes ក្នុង MVC

នៅ MVC, attributes type HTTP verb សំខាន់បំផុតសម្រាប់បំបែក **GET និង POST methods** នៅក្នុង controller ដើម្បីបម្រើ **Views និង form submissions**។

### 1️⃣ `[HttpGet]`

- ប្រើសម្រាប់ **បង្ហាញ page** ឬ form view.
- Method GET មិនគួរតែផ្លាស់ប្តូរ data.

```csharp id="mvc1"
public class AccountController : Controller
{
    [HttpGet]
    public IActionResult Login()
    {
        // Show login form
        return View();
    }
}
```

> URL `/Account/Login` ជាមួយ GET request នឹង show login page.

---

### 2️⃣ `[HttpPost]`

- ប្រើសម្រាប់ **process form submission** ឬ perform action.
- Method POST អាចផ្លាស់ប្តូរ database.

```csharp id="mvc2"
[HttpPost]
public IActionResult Login(string username, string password)
{
    if (username == "admin" && password == "123")
    {
        // login success
        return RedirectToAction("Dashboard");
    }
    ModelState.AddModelError("", "Invalid credentials");
    return View();
}
```

> POST request នឹង handle login form submission.

---

### 3️⃣ GET + POST same action name

MVC pattern ពេញនិយមសម្រាប់ form:

```csharp id="mvc3"
public class ProductController : Controller
{
    // Show create product form
    [HttpGet]
    public IActionResult Create()
    {
        return View();
    }

    // Handle form submission
    [HttpPost]
    public IActionResult Create(Product model)
    {
        if(ModelState.IsValid)
        {
            // Save product to database
            return RedirectToAction("List");
        }
        return View(model);
    }
}
```

> នៅទីនេះ, **Create()** មាន **GET** និង **POST** action methods.
>
> - GET → show form
> - POST → process form submission

---

### 4️⃣ Other Verbs in MVC

- PUT / DELETE / PATCH **មិនប្រើទូទៅនៅ MVC views**, ព្រោះ browser forms support mainly GET & POST.
- អ្នកអាចប្រើ them ជាមួយ **JavaScript/Ajax** បើចង់, តែក្នុង MVC traditional, ចម្បងគឺ GET និង POST.

---

### 🔹 Summary (MVC context)

| Attribute                     | Use in MVC                                |
| ----------------------------- | ----------------------------------------- |
| `[HttpGet]`                   | Show pages / forms                        |
| `[HttpPost]`                  | Handle form submissions                   |
| `[AcceptVerbs("GET","POST")]` | Rarely, for flexibility (both GET & POST) |

---
